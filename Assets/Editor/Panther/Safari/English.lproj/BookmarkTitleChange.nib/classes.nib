{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {acceptNewBookmark = id; cancelNewBookmark = id; }; 
            CLASS = NewBookmarkController; 
            LANGUAGE = ObjC; 
            OUTLETS = {locationPopUp = NSPopUpButton; titleField = NSTextField; }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {
            ACTIONS = {acceptNewBookmark = id; cancelNewBookmark = id; }; 
            CLASS = NewBookmarksController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                iconView = NSImageView; 
                locationPopUp = NSPopUpButton; 
                promptField = NSTextField; 
                titleField = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}